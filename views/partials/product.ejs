<div class="flex flex-col items-center gap-3">
    <a href="/noFace/produto/<%= product.id %>" class="w-52 rounded-xl border border-black hover:opacity-85"><img src="<%= product.img %>" alt="" class="rounded"/></a>
    <p class="text-xl font-bold"><%= product.name %></p>
    <form action="/noFace/produto/<%= product.id %>" method="post">
        <button class="rounded-lg border border-black hover:bg-opacity-85 bg-black text-white shadow-md w-32 py-1 mt-2">Comprar</button>
    </form>
</div>
