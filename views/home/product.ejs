<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.75">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="stylesheet" href="/css/assets/style.css">
        <script src="/js/users/menu.js"></script>
        <title>NoFace | Produto</title>
    </head>
    <%- include('../partials/header.ejs') %>
    <div class="mx-auto flex max-w-[40rem] flex-col items-center divide-gray-200 pt-14 pb-20 md:max-w-[70rem] xl:max-w-[100rem] md:flex-row md:divide-x-2">
        <p class="text-2xl sm:text-3xl font-bold md:hidden"><%= nome %></p>
        <div class="mt-3 md:mt-0 md:w-[40%]">
            <img src="<%= img %>" alt="" class="mx-auto h-64 sm:h-80 md:h-96 p-2" />
        </div>
        <div class="px-10 md:w-[60%] w-full">
            <p class="hidden text-4xl font-bold md:block"><%= nome %></p>
            <div class="mt-4 md:mt-7 text-center md:text-left">
                <div class="flex w-fit mx-auto md:mx-0 text-sm md:text-base items-center divide-x-2 divide-gray-500 rounded border border-gray-500 font-semibold">
                    <p class="px-4 py-1"><%= variacao %></p>
                </div>
                <p class="mt-4 md:mt-6 text-xl sm:text-2xl font-bold">R$ <%= preco %></p>
                <button class="mt-7 w-52 rounded-lg bg-black py-1.5 text-white shadow-md hover:bg-opacity-85">Comprar</button>
            </div>
    
            <details class="group mt-12 md:mt-8 text-sm" open>
                <summary class="text-lg font-semibold">Descrição</summary>
                <p class="mt-2"><%= descricao %></p>
            </details>
        </div>
    </div>
    <%- include('../partials/footer.ejs') %>
