<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.75">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="stylesheet" href="/css/assets/style.css">
        <link rel="stylesheet" href="/css/home/shop.css">
        <link rel="shortcut icon" href="/images/dark_noFaceLogo.png">

        <script src="/js/users/menu.js"></script>
        <title>NoFace | Home</title>
    </head>
    <%- include('../partials/header.ejs') %>
<main id="main">


        <% let produtosEncontrados = 0; %>

        <% categorias.forEach(categoria => { %>
            <% if (produtosPorCategoria[categoria.id] && produtosPorCategoria[categoria.id].length > 0) { %>
                <% produtosEncontrados++; %>
                <section class="section">
                    <div class="title-container">
                        <h2><%= categoria.nome %></h2>
                    </div>
                    <div class="menu-items">
                        <% produtosPorCategoria[categoria.id].forEach(produto => { %>
                            <div class="menu-item">
                                <img src="<%= produto.img ? produto.img : 'images/produtos/default.webp' %>" alt="<%= produto.nome %>" />
                                <p class="nome"><%= produto.nome %></p>
                                <% if (produto.descricao) { %>
                                    <p class="descricao"><%= produto.descricao %></p>
                                <% } else { %>
                                    <p class="descricao" style="visibility: hidden;">&nbsp;</p>
                                <% } %>
                                <p>R$ <%= produto.preco.toFixed(2) %></p>
                                <button id="adicionarBtn" onclick="addToCart(<%= JSON.stringify() %>, '<%= produto.id %>')">Adicionar</button>
                            </div>
                        <% }) %>
                    </div>
                </section>
            <% } %>
        <% }) %>

        <% if (produtosEncontrados === 0) { %>
            <div class="no-products">
                <img src="/images/noProduct.png" alt="Nenhum produto encontrado" />
                <p>Ops! Houve um erro ao buscar produtos. Tente novamente mais tarde</p>
            </div>
        <% } %>
    </main>


    <div class="mt-10 bg-gray-950 md:px-12 py-12 px-8 relative">
        <div class="mx-auto md:max-w-[100rem] max-w-[50rem]">
            <div class="text-center w-[70%]">
                <h1 class="text-2xl font-bold sm:text-xl lilita-one-regular text-white ">Explore a essência urbana em cada detalhe</h1>
                <p class="text-lg mt-1 libre-franklin mb-5 text-white ">Fortalecendo a autoexpressão através do estilo e do lifestyle urbano</p>
                <a href="/portfolio" class="w-fit rounded-lg border border-white bg-white py-2 px-5 black-button hover:bg-gray-950/80 hover:text-white">Conheça nosso portfólio</a>
            </div>
            <img src="/images/homePageModel.png" alt="Modelo 3D" class="absolute w-52 bottom-0 right-72"/>
        </div>
    </div>
    <%- include('../partials/footer.ejs') %>
